#pragma once

// for the time being will probably just be AES-256
enum class Alorithms{
  
};


// include data such as your file signature, version, alorithm, salt, nonce, and mac
struct EncryptionInfo{

};
